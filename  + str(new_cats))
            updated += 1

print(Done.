(eval):19: file name too long: $select=id,subject,categories, headers=hdrs)\n    msgs = json.loads(urllib.request.urlopen(req2).read())\n    for msg in msgs.get(value, []):\n        mid = msg.get(id, )\n        old_cats = msg.get(categories, [])\n        new_cats = [tag_map.get(c, c) for c in old_cats]\n        if new_cats != old_cats:\n            data = json.dumps({categories: new_cats}).encode()\n            req3 = urllib.request.Request(base + /messages/ + mid, data=data, headers=hdrs, method=PATCH)\n            urllib.request.urlopen(req3)\n            subj = msg.get(subject, )[:50]\n            print(Updated:
